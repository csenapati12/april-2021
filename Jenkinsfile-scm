pipeline{
agent any

stages{
stage("prepare"){
steps{
 script{
// properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '', daysToKeepStr: '3', numToKeepStr: ''))])
 properties([[$class: 'BuildBlockerProperty', blockingJobs: '', useBuildBlocker: false], [$class: 'DatadogJobProperty'], [$class: 'RebuildSettings', autoRebuild: false, rebuildDisabled: false], parameters([gitParameter(branch: '', branchFilter: '.*', defaultValue: '', description: '', name: 'branch', quickFilterEnabled: false, selectedValue: 'NONE', sortMode: 'NONE', tagFilter: '*', type: 'PT_BRANCH'), [$class: 'NodeParameterDefinition', allowedSlaves: [], defaultSlaves: [], description: '', name: 'node', nodeEligibility: [$class: 'AllNodeEligibility'], triggerIfResult: 'multiSelectionDisallowed']])])

 }
}
}
 stage("checkout"){
 steps{
// checkout scm
echo "Hello"
 }
 }
}
}
